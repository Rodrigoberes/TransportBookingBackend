# =============================================================================
# ENVIRONMENT CONFIGURATION - SECURITY BEST PRACTICES
# =============================================================================
#
# 🚨 SECURITY WARNING:
# Never commit real credentials to version control!
# Use this file as a template and create your own .env file locally.
#
# For production deployments:
# - Use platform environment variables (Render, Railway, etc.)
# - Never expose database passwords in code/config files
# - Rotate credentials regularly
# =============================================================================

# Database Configuration
# For local development:
DATABASE_URL=postgresql://user:password@localhost:5432/transport_booking?sslmode=disable

# For Supabase (local development - use connection pooling):
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres?pgbouncer=true

# For production: Set via platform environment variables
# Render: Dashboard → Service → Environment → Add Environment Variable
# Railway: Dashboard → Project → Variables
# Heroku: heroku config:set DATABASE_URL="..."

# Server Configuration
PORT=8080
ENVIRONMENT=development

# JWT Configuration
# Generate secure secret: openssl rand -base64 32
JWT_SECRET=your-secure-random-jwt-secret-here

# Supabase Configuration (optional - for additional features)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# PRODUCTION SECURITY CHECKLIST:
# ✅ Use environment variables for all secrets
# ✅ Never commit .env files with real credentials
# ✅ Use connection pooling for database connections
# ✅ Rotate credentials regularly
# ✅ Use least-privilege database users
# ✅ Enable SSL/TLS for all connections
# =============================================================================